/* Inicializamos el contador en 0, luego cuando hacemos el ciclo while
le mandamos una referencia a la variable indicandole, que si contador es menor a 10
pues aunmentemos el valor de contador, por ende cuando llegue a 10, el Ciclo(loop)
se detendra. */


/* Descomenta las lineas de codigo que veas comentadas, para ver el comportamiento del while*/

let contador = 0;

/* while(contador < 10) {
    contador++;
    console.log(contador);
} */

//Esta genera un bubcle infinito, porque no sabra donde tenerse, seria algo irracional.
/* while(contador < 10) {
    contador++;
    console.log(contador);
    while(contador >5){
        contador --;
        console.log(contador);
    }
} */